# 定时获取汇率并推送到钉钉
# 需在仓库 Settings → Secrets and variables → Actions 中配置：
#   DINGTALK_WEBHOOK_URL - 钉钉机器人 Webhook 地址
#   DINGTALK_SECRET - 加签密钥（未启用可留空或创建同名 Secret 填空）

name: CNY/IDR 汇率推送

on:
  push:
    branches: [main, master]
  schedule:
    # 每天北京时间 9:00（1:00 UTC），可按需修改 cron
    - cron: '0 1 * * *'
    - cron: '0 5 * * *'
  workflow_dispatch:  # 支持手动触发

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: rate_log.json
          key: rate-log-${{ runner.os }}

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 检查钉钉配置
        run: |
          if [ -z "${{ secrets.DINGTALK_WEBHOOK_URL }}" ]; then
            echo "::error::未配置 DINGTALK_WEBHOOK_URL，请在仓库 Settings → Secrets → Actions 中添加"
            exit 1
          fi
          echo "钉钉 Webhook 已配置，开始推送"

      - name: 运行推送
        env:
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        run: python main.py
